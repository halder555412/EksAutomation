#apiVersion: v1
#kind: Service
#metadata:
#  name: eks-automation-service
#spec:
#  type: LoadBalancer
#  selector:
#    app: eks-automation
#  ports:
#    - port: 80
#      targetPort: 9000
#

apiVersion: v1
kind: Service

metadata:
  name: eks-automation-service

spec:
  type: NodePort

  selector:
    app: eks-automation
  ports:
    - port: 9000
      targetPort: 9000
      nodePort: 30007